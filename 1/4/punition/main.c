#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int	main(int ac, char **av)
{
  int	input;
  
  scanf("%d", &input);
  ac = ac;
  av = av;
  while (input-- > 0)
      printf("Je dois suivre en cours\n");
    return (0);
}
